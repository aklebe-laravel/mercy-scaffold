<?php

/**
 * Boot config
 */
return [
    /**
     * Declare the env file you want to load by HTTP bootstrap.
     * It will be effecting the http only but not queue, schedule and console.
     */
    'env_file' => '.env',
    // 'env_file' => '.env.dusk.local',
];
